export enum PosType {
  TCPOS = 'tcpos',
  INTERNAL = 'internal',
}

export enum PosOrderStatus {
  DRAFT = 'Draft',
  ORDERED = 'Ordered',
  CONFIRMED = 'Confirmed',
  CLOSED = 'Closed',
  CLOSED_AND_TRANSACTION = 'Closed And Transaction',
}
